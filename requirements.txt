# Backend (FastAPI is the primary application entry point)
fastapi>=0.110
uvicorn[standard]>=0.27

# Supabase / PostgreSQL - the primary database backend
psycopg[binary]>=3.1

# Optional: only if you prefer python-dotenv over the built-in .env reader
# python-dotenv>=1.0

# Note: backend/server.py and the test suite run on the standard library
# alone, so `DATABASE_BACKEND=sqlite python run_demo.py` works with nothing
# installed. That path exists as an offline fallback, not as the default.
